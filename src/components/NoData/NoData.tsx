import Title from '@/components/Title/Title';

const NoData = () => (
  <div
    className='rounded-xl px-3 w-full max-[480px]:p-0 max-[680px]:px-0'>
      <Title text='No data' tag='h1' className='text-center mt-20 font-bold text-lg'/>
  </div>
);

export default NoData;
